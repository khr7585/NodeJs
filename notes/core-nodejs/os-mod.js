//1.os.platform()-Returns the operating system platform.
// const os=require("os");
// console.log(os.platform());

//2.os.arch()-returns the cpu architecture
// const os=require("os");
// console.log(os.arch());

//3.os.hostname()-returns the computer's hostname
// const os=require("os");
// console.log(os.hostname());

//4.os.homedir()-returns the current users home directory
// const os=require("os");
// console.log(os.homedir());

//5.os.userinfo()-returns the information about the current user
// const os=require("os");
// console.log(os.userInfo());

//6.os.totalmem()-returns the total system memory(in bytes).
// const os=require("os");
// console.log(os.totalmem());

// const os = require("os");
// console.log((os.totalmem() / 1024 / 1024 / 1024).toFixed(2), "GB");

//7.os.freemem()-return the available free memory 
// const os=require("os");
// console.log(os.freemem());

// const os = require("os");
// console.log((os.freemem() / 1024 / 1024).toFixed(2), "MB");

//8.os.uptime()-Returns how long the system has been running (in seconds).
// const os=require("os");
// console.log(os.uptime());

//9.os.cpus()-Returns information about all CPU cores.
// const os=require("os");
// console.log(os.cpus());